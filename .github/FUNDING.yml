# Support leashd development.
# https://docs.github.com/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/displaying-a-sponsor-button-in-your-repository

# GitHub only renders the "Sponsor" button for recognised platforms or https
# `custom` URLs — a bare `lightning:` URI is dropped and kills the whole button.
# So the custom link points at the site, where the Lightning donate button lives.
# TODO: replace the Lightning address in apps/web brand.ts (DONATE_LIGHTNING).
custom:
  - "https://leashd.dev"
# Enable once a GitHub Sponsors profile exists:
# github: brainbytes-dev
